public class Taxi extends PublicTraffic {
    public Taxi(int num, boolean status, int peopleMax, int currentPassenger, int fuelVolume, int speed) {
        super(num, status, peopleMax, currentPassenger, fuelVolume, speed);
    }
}
